<% content_for(:page_title) { "Edit #{@server_rack.name}" } %>

<div class="mb-6">
  <nav class="text-sm text-slate-500 mb-2">
    <%= link_to "Racks", server_racks_path, class: "hover:text-teal-600" %> /
    <%= link_to @server_rack.name, server_rack_path(@server_rack), class: "hover:text-teal-600" %> /
    <span class="text-slate-700">Edit</span>
  </nav>
  <h1 class="text-2xl font-bold text-slate-900">Edit <%= @server_rack.name %></h1>
  <p class="text-sm text-slate-500 mt-1">Modify rack information</p>
</div>

<%= render "form" %>
