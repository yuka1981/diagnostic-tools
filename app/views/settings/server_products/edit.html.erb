<% content_for(:page_title) { "Edit #{@server_product.name}" } %>

<div class="mb-6">
  <nav class="text-sm text-slate-500 mb-2">
    <%= link_to "Server Products", settings_server_products_path, class: "hover:text-teal-600" %> /
    <%= link_to @server_product.name, settings_server_product_path(@server_product), class: "hover:text-teal-600" %> /
    <span class="text-slate-700">Edit</span>
  </nav>
  <h1 class="text-2xl font-bold text-slate-900">Edit <%= @server_product.name %></h1>
</div>

<%= render "form" %>
