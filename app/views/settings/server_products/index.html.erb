<% content_for(:page_title) { "Server Products" } %>

<div class="flex items-center justify-between mb-6">
  <div>
    <h1 class="text-2xl font-bold text-neutral-85">Server Products</h1>
    <p class="text-sm text-neutral-45 mt-1">Manage QCT server product catalog</p>
  </div>
  <div class="flex items-center gap-3">
    <%= button_to sync_settings_server_products_path, method: :post, class: "btn-secondary", data: { turbo_confirm: "Start sync from QCT website?" } do %>
      <%= lucide_icon("refresh-cw", class: "h-4 w-4 mr-2") %>
      Sync from QCT
    <% end %>
    <%= link_to new_settings_server_product_path, class: "btn-primary" do %>
      <%= lucide_icon("plus", class: "h-4 w-4 mr-2") %>
      Add Product
    <% end %>
  </div>
</div>

<% if @last_sync %>
  <div class="mb-4 text-sm text-neutral-45">
    Last synced <%= time_ago_in_words(@last_sync.completed_at) %> ago
    <% if @last_sync.products_added > 0 || @last_sync.products_updated > 0 %>
      (<%= @last_sync.products_added %> added, <%= @last_sync.products_updated %> updated)
    <% end %>
  </div>
<% end %>

<div class="card-netbox mb-4">
  <%= form_with url: settings_server_products_path, method: :get, class: "flex flex-wrap gap-4 p-4", data: { turbo_frame: "_top" } do |f| %>
    <div class="flex-1 min-w-[200px]">
      <%= f.text_field :q, value: params[:q], placeholder: "Search by name...", class: "form-input w-full" %>
    </div>
    <div class="w-48">
      <%= f.select :series,
            options_for_select(
              ServerProduct.series_options.map { |s| [s, s] },
              params[:series]
            ),
            { include_blank: "All Series" },
            class: "form-select w-full" %>
    </div>
    <div class="w-48">
      <%= f.select :form_factor,
            options_for_select(
              ServerProduct.form_factor_options.map { |ff| [ff, ff] },
              params[:form_factor]
            ),
            { include_blank: "All Form Factors" },
            class: "form-select w-full" %>
    </div>
    <div class="flex gap-2">
      <%= f.submit "Filter", class: "btn-primary" %>
      <%= link_to "Clear", settings_server_products_path, class: "btn-secondary" %>
    </div>
  <% end %>
</div>

<div class="card-netbox">
  <% if @server_products.any? %>
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-neutral-8 text-sm">
        <thead class="bg-neutral-2">
          <tr>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b w-16">Image</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">Model Name</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">Series</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">Form Factor</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">CPU Support</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">Max Memory</th>
            <th scope="col" class="px-3 py-2 text-left text-xs font-bold uppercase tracking-wider text-neutral-45 border-b">Last Synced</th>
            <th scope="col" class="relative px-3 py-2 border-b">
              <span class="sr-only">Actions</span>
            </th>
          </tr>
        </thead>
        <turbo-frame id="products-table-body">
          <%= render "products_table" %>
        </turbo-frame>
      </table>
    </div>

    <% if @server_products.total_pages > 1 %>
      <div class="border-t border-neutral-8 bg-neutral-2 px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="text-sm text-neutral-85">
            Showing
            <span class="font-medium"><%= @server_products.offset_value + 1 %></span>
            to
            <span class="font-medium"><%= [@server_products.offset_value + @server_products.size, @server_products.total_count].min %></span>
            of
            <span class="font-medium"><%= @server_products.total_count %></span>
            results
          </div>
          <%= paginate @server_products, params: params.permit(:series, :form_factor, :q).to_h, window: 2, outer_window: 1 %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="flex flex-col items-center justify-center py-12 text-center">
      <div class="h-12 w-12 rounded-full bg-neutral-4 flex items-center justify-center mb-4">
        <%= lucide_icon("server", class: "h-6 w-6 text-neutral-25") %>
      </div>
      <p class="text-sm font-medium text-neutral-85">No server products yet</p>
      <p class="mt-1 text-xs text-neutral-45">Add a product manually or sync from QCT</p>
      <div class="mt-4 flex gap-2">
        <%= button_to sync_settings_server_products_path, method: :post, class: "btn-secondary" do %>
          Sync from QCT
        <% end %>
        <%= link_to new_settings_server_product_path, class: "btn-primary" do %>
          Add Product
        <% end %>
      </div>
    </div>
  <% end %>
</div>
