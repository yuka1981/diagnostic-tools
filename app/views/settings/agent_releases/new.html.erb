<% content_for(:page_title) { "New Agent Release" } %>
<% content_for(:breadcrumbs) do %>
  <span class="mx-2">/</span>
  <%= link_to "Settings", settings_agent_path, class: "hover:text-teal-600 font-bold" %>
  <span class="mx-2">/</span>
  <%= link_to "Agent Config", settings_agent_path, class: "hover:text-teal-600 font-bold" %>
  <span class="mx-2">/</span>
  <span class="font-medium text-slate-700">New Release</span>
<% end %>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-900">New Agent Release</h1>
  <p class="text-sm text-slate-500 mt-1">Create a new version of the HPC agent binary</p>
</div>

<%= render "creation_form", agent_release: @agent_release, go_available: @go_available, go_version: @go_version %>
