<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Diagnostic Tools" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-neutral-2 dark:bg-neutral-95">
    <%# Flash messages %>
    <% if notice.present? %>
      <div class="fixed top-4 right-4 z-50 max-w-sm">
        <div class="rounded-lg bg-success-1 p-4 shadow-lg dark:bg-success-9/50">
          <div class="flex">
            <div class="flex-shrink-0">
              <%= lucide_icon("circle-check", class: "h-5 w-5 text-success-5") %>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-success-8 dark:text-success-2"><%= notice %></p>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="fixed top-4 right-4 z-50 max-w-sm">
        <div class="rounded-lg bg-error-1 p-4 shadow-lg dark:bg-error-9/50">
          <div class="flex">
            <div class="flex-shrink-0">
              <%= lucide_icon("circle-x", class: "h-5 w-5 text-error-5") %>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-error-8 dark:text-error-2"><%= alert %></p>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
