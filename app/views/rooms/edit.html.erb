<% breadcrumb_trail([
  { title: "Sites", path: sites_path },
  { title: @room.site.name, path: site_path(@room.site) },
  { title: @room.name, path: room_path(@room) },
  { title: "Edit" }
]) %>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-900">Edit <%= @room.name %></h1>
  <p class="text-sm text-slate-500 mt-1">Modify room information</p>
</div>

<%= render "form" %>
