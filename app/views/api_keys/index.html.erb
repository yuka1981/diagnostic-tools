<% content_for(:page_title) { "API Key Management" } %>

<div class="flex items-center justify-between mb-6">
  <div>
    <h1 class="text-2xl font-bold text-slate-900">API Key Management</h1>
  </div>
  <%= link_to new_api_key_path, class: "btn-primary", data: { turbo_frame: "api_key_modal" } do %>
    <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
    </svg>
    Generate New Key
  <% end %>
</div>

<div class="card-netbox">
  <% if @api_keys.any? %>
<%# ... table content ... %>
  <% end %>
</div>

<%= turbo_frame_tag "api_key_modal" %>
