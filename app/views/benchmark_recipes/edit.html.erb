<% content_for(:page_title) { "Edit #{@benchmark_recipe.display_name}" } %>

<div class="mb-6">
  <nav class="text-sm text-slate-500 mb-2">
    <%= link_to "Benchmark Recipes", benchmark_recipes_path, class: "hover:text-teal-600" %> /
    <%= link_to @benchmark_recipe.name, benchmark_recipe_path(@benchmark_recipe), class: "hover:text-teal-600" %> /
    <span class="text-slate-700">Edit</span>
  </nav>
  <h1 class="text-2xl font-bold text-slate-900">Edit <%= @benchmark_recipe.display_name %></h1>
  <p class="text-sm text-slate-500 mt-1">Modify benchmark configuration</p>
</div>

<%= render "form" %>
